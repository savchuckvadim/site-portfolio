import { Badge } from "@/components/ui/badge"
import { memo } from "react"

export default memo(function ContactsBadge() {
    return <Badge variant="outline" className="px-3 py-1">
        Contact
    </Badge>
})
